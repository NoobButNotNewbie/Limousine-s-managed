# Server Configuration
PORT=3000
NODE_ENV=development

# Database (PostgreSQL)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Replace 'password' with your actual postgres password
DATABASE_URL=postgresql://postgres:password@localhost:5432/limousine

# Redis (for Seat Locking & OTP)
# Format: redis://HOST:PORT
REDIS_URL=redis://localhost:6379

# Email Service (SMTP)
# Use your email provider details (e.g., Gmail App Password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM="Limousine Booking <no-reply@limo.com>"
